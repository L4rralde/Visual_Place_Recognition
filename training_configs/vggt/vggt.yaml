#define: &process_res 518 #This is the largest dimension.
define: &process_res 294

backbone_arch: "vggt"

input_config:
  img_size: *process_res #Actually, this one is not used at all.

backbone_config:
  frozen: true
  return_token: true

agg_config:
  #num_channels from dinov2-large
  num_clusters: 64
  cluster_dim: 128
  token_dim: 256

max_epochs: 5
