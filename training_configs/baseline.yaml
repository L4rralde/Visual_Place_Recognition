backbone_arch: "dinov2_vitb14"

input_config:
  img_size: [224, 224]

backbone_config:
  num_trainable_blocks: 4
  norm_layer: true

agg_config:
  #num_channels from dinov2-base: 768
  num_clusters: 64
  cluster_dim: 128
  token_dim: 256

max_epochs: 10
