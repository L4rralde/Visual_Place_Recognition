backbone_arch: "dinov3_vitb16"

input_config:
  img_size: [256, 256]

backbone_config:
  num_trainable_blocks: 2
  norm_layer: true
  rope_sincos: true

agg_config:
  #num_channels from dinov3-base: 768 (Same as dinov2-Base)
  num_clusters: 64
  cluster_dim: 128
  token_dim: 256

max_epochs: 8
