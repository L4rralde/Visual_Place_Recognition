define: &process_res 322 #This is the largest dimension.

backbone_arch: "da3-base"

input_config:
  img_size: *process_res #Actually, this one is not used at all.

backbone_config:
  frozen: true

agg_config:
  #num_channels from dinov3-base: 768 (Same as dinov2-Base)
  num_clusters: 64
  cluster_dim: 128
  token_dim: 256

max_epochs: 10
