backbone_arch: "da3-base"

input_config:
  img_size: [224, 224]

backbone_config:
  return_token: true

agg_config:
  #num_channels from dinov3-base: 768 (Same as dinov2-Base)
  num_clusters: 64
  cluster_dim: 128
  token_dim: 256

max_epochs: 10
